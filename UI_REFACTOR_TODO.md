# UI Refactor TODO

当前策略：保留旧版 UI，默认进入旧版；新版 UI 通过切换按钮逐步接入。

## 已完成

- [x] 新增 `旧版 UI / 新版 UI` 切换入口
- [x] 切换状态持久化到本地存储，默认 `旧版 UI`
- [x] 新版外层壳子已接入：左侧图标导航、顶部品牌区、语言切换
- [x] `IV 计算器` 输入框先改回整串输入样式
- [x] 校准页右栏已拆成“校准对照 / 校准结果表”两块
- [x] 新版下隐藏 `showSeeds` 按钮
- [x] 新版下 `Trainer ID / Secret ID` 已提前到基础参数区域
- [x] 新版下中间主区已经分成三个真实卡片区
- [x] 左侧动态修正工具已固定高度，调整日志开始独立滚动

## 下一步优先级

- [ ] 继续精修 `CalibrationForm.tsx` 新版三栏布局的列宽和滚动体验
- [ ] 把右侧两个表的字段和逻辑进一步往旧版样式对齐，只改位置和样式
- [ ] 继续压缩左侧上半部分，让 `调整日志` 再多显示几行
- [ ] 最后确认 `调整日志` 当前列是否完全符合你的预期
- [ ] 把 `Seed 容差` 的新版视觉再强化成单输入框 `±` 语义
- [ ] 清理新版校准页里剩余不该出现的辅助说明和默认 MUI 观感

## IV 计算器约束

- [x] 顶部保留单条整串输入，例如 `50 162 121 106 143 93 96`
- [x] 不拆成多个小格输入
- [x] 不额外标注每个数字分别对应什么
- [x] 不显示单独的“计算”按钮，继续自动计算
- [x] 下方范围结果保持“最小值 - 最大值 - 重置”结构
- [ ] 继续把结果区视觉压到更接近参考图的状态

## 其他页面

- [x] `搜索器 / ID组合 / 初始SEED / 宾果` 先不改布局逻辑
- [~] 已开始统一视觉风格到新版外壳，后续继续补细节

## 修改原则

- [ ] 每次只打一小块补丁，避免 Windows 命令长度问题
- [ ] 优先做可中断续做的结构性改动
- [ ] 旧版逻辑在你确认稳定前不删除
